# Sender app
Rewards a user on signup with $5 of AI credits.

```
npm install
TORI_API_KEY=tori_sk_... USER_EMAIL=you@example.com npm start
```
The user gets a claim email; their first sign-in fires your program's "first reward claimed" milestone.
