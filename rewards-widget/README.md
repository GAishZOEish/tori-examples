# Rewards widget
Your users hold points in *your* system; redeeming turns them into AI credits through Tori.

```
npm install
TORI_API_KEY=tori_sk_... npm start   # open http://localhost:3456
```
Every redemption routed through your key is attributed to your integration — the developer program's "first redemption routed" milestone.
